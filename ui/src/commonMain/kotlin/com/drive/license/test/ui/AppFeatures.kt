package com.drive.license.test.ui

object AppFeatures {
    /** Driving-school list on Home + dedicated screen (no map). */
    const val drivingSchoolsEnabled = true
    /** Ishihara-style color-vision practice (separate from theory questions). */
    const val colorVisionTestEnabled = true
    const val aiEnabled = false
    /** Context-aware copy on home, in-test milestones, and results. */
    const val motivationEnabled = true
}
