CREATE TABLE QuestionCategory (
    id INTEGER NOT NULL PRIMARY KEY,
    name TEXT NOT NULL
);

selectAll:
SELECT * FROM QuestionCategory;

selectById:
SELECT * FROM QuestionCategory WHERE id = ?;

selectByName:
SELECT * FROM QuestionCategory WHERE name = ?;

insertCategory:
INSERT INTO QuestionCategory (name) VALUES (?);

deleteAllCategories:
DELETE FROM QuestionCategory;